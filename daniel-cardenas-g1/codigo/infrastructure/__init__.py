"""Infrastructure layer — Adapters concretos (config, clock, pricing, API).

Esta capa implementa los ports definidos en application/ports/ y proporciona
los adapters para HTTP (FastAPI) y UI (Streamlit).
"""
